const trappingRainWater = require('./03.js');

describe('trapping rain water', () => {
  test('', () => {
    expect(trappingRainWater('', '')).toBe();
  });

  test('', () => {
    expect(trappingRainWater('', '')).toBe();
  });

  test('', () => {
    expect(trappingRainWater('', '')).toBe();
  });

  test('', () => {
    expect(trappingRainWater('', '')).toBe();
  });
});