const backspaceCompare = require('./04.js');

describe('backspace compare', () => {
  test('', () => {
    expect(backspaceCompare('', '')).toBe();
  });

  test('', () => {
    expect(backspaceCompare('', '')).toBe();
  });

  test('', () => {
    expect(backspaceCompare('', '')).toBe();
  });

  test('', () => {
    expect(backspaceCompare('', '')).toBe();
  });
});